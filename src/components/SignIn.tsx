import React from "react";
import {
  GoogleAuthProvider,
  getAuth,
  signInWithPopup,
  User,
  signInWithCredential,
  signInWithRedirect,
  signInAnonymously,
} from "firebase/auth";

function SignIn() {
  return <div>SignIn</div>;
}

export default SignIn;
